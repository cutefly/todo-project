# Todo 수정 기능 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 각 Todo 행의 ✏ 버튼을 클릭하면 모달이 열려 제목·우선순위·마감일·카테고리를 수정할 수 있다.

**Architecture:** 기존 PATCH `/api/todos/[id]` 엔드포인트를 그대로 활용한다. `EditTodoModal` 컴포넌트(AddTodoForm 패턴 동일)를 신규 생성하고, TodoItem에 ✏ 버튼과 `onEdit` 콜백을 추가한 뒤 page.tsx에서 `editingTodo` 상태로 모달을 제어한다.

**Tech Stack:** Next.js 14 App Router, TypeScript, Tailwind CSS (Spotify 디자인 토큰), Playwright E2E

---

## File Map

| 파일 | 변경 |
|------|------|
| `components/EditTodoModal.tsx` | 신규 생성 |
| `components/TodoItem.tsx` | `onEdit` prop + ✏ 버튼 + 6열 그리드 |
| `components/TodoList.tsx` | `onEdit` prop 전파 + 헤더 6열 그리드 |
| `app/page.tsx` | `editingTodo` 상태, `handleEditSave`, `<EditTodoModal>` 렌더링 |
| `tests/e2e/todo.spec.ts` | 수정 기능 E2E 테스트 3개 추가 |

---

## Task 1: EditTodoModal 컴포넌트 생성

**Files:**
- Create: `components/EditTodoModal.tsx`

- [ ] **Step 1: `components/EditTodoModal.tsx` 파일 생성**

```tsx
// components/EditTodoModal.tsx
'use client'
import { useState } from 'react'
import type { Category, Priority, Todo } from '@/types'

interface EditTodoModalProps {
  todo: Todo
  categories: Category[]
  onSave: (
    id: string,
    data: { title: string; priority: Priority; dueDate: string | null; categoryId: string | null }
  ) => Promise<boolean>
  onClose: () => void
}

const PRIORITY_OPTIONS: { value: Priority; label: string }[] = [
  { value: 'HIGH',   label: '높음' },
  { value: 'MEDIUM', label: '중간' },
  { value: 'LOW',    label: '낮음' },
]

export function EditTodoModal({ todo, categories, onSave, onClose }: EditTodoModalProps) {
  const [title, setTitle]       = useState(todo.title)
  const [priority, setPriority] = useState<Priority>(todo.priority)
  const [dueDate, setDueDate]   = useState(todo.dueDate ? todo.dueDate.slice(0, 10) : '')
  const [categoryId, setCategoryId] = useState(todo.categoryId ?? '')
  const [loading, setLoading]   = useState(false)

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    if (!title.trim()) return
    setLoading(true)
    const success = await onSave(todo.id, {
      title: title.trim(),
      priority,
      dueDate: dueDate ? new Date(dueDate).toISOString() : null,
      categoryId: categoryId || null,
    })
    setLoading(false)
    if (success) onClose()
  }

  return (
    <div
      className="fixed inset-0 bg-black/70 flex items-center justify-center z-20 p-4"
      onClick={(e) => e.target === e.currentTarget && onClose()}
    >
      <form
        onSubmit={handleSubmit}
        className="bg-spotify-surface rounded-lg p-6 w-full max-w-md shadow-spotify-heavy border border-spotify-card"
      >
        <h2 className="text-sp-lg font-bold text-white mb-5">할 일 수정</h2>

        <input
          autoFocus
          type="text"
          placeholder="할 일을 입력하세요"
          value={title}
          onChange={(e) => setTitle(e.target.value)}
          className="w-full bg-spotify-elevated rounded-pill px-5 py-2.5 text-white text-sp-base placeholder-spotify-muted mb-3 focus:outline-none"
          style={{ boxShadow: 'rgb(124,124,124) 0px 0px 0px 1px inset' }}
        />

        <div className="grid grid-cols-2 gap-3 mb-3">
          <select
            value={priority}
            onChange={(e) => setPriority(e.target.value as Priority)}
            className="bg-spotify-elevated border border-spotify-border rounded-pill px-4 py-2 text-white text-sp-sm focus:outline-none"
          >
            {PRIORITY_OPTIONS.map(({ value, label }) => (
              <option key={value} value={value}>{label}</option>
            ))}
          </select>
          <input
            type="date"
            value={dueDate}
            onChange={(e) => setDueDate(e.target.value)}
            className="bg-spotify-elevated border border-spotify-border rounded-pill px-4 py-2 text-spotify-muted text-sp-sm focus:outline-none"
          />
        </div>

        {categories.length > 0 && (
          <div className="mb-5">
            <p className="text-sp-xs text-spotify-muted uppercase tracking-sp-nav mb-2">카테고리</p>
            <div className="flex flex-wrap gap-2">
              {categories.map((cat) => (
                <button
                  key={cat.id}
                  type="button"
                  onClick={() => setCategoryId(categoryId === cat.id ? '' : cat.id)}
                  className={`rounded-pill px-3 py-1 text-sp-sm font-bold transition-colors ${
                    categoryId === cat.id
                      ? 'text-white'
                      : 'bg-spotify-elevated border border-spotify-border text-spotify-muted hover:text-white'
                  }`}
                  style={categoryId === cat.id ? { background: cat.color } : undefined}
                >
                  {cat.name}
                </button>
              ))}
            </div>
          </div>
        )}

        <div className="flex gap-3 justify-end">
          <button
            type="button"
            onClick={onClose}
            className="px-5 py-2 rounded-pill border border-[#7c7c7c] text-white text-sp-sm font-bold uppercase tracking-sp-btn hover:border-white transition-colors"
          >
            취소
          </button>
          <button
            type="submit"
            disabled={loading || !title.trim()}
            className="px-6 py-2 rounded-pill-lg bg-spotify-green text-black text-sp-sm font-bold uppercase tracking-sp-btn hover:bg-[#1fdf64] disabled:opacity-50 transition-colors"
          >
            저장
          </button>
        </div>
      </form>
    </div>
  )
}
```

- [ ] **Step 2: TypeScript 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add components/EditTodoModal.tsx
git commit -m "feat: add EditTodoModal component"
```

---

## Task 2: TodoItem ✏ 버튼 추가 및 TodoList 연결

**Files:**
- Modify: `components/TodoItem.tsx`
- Modify: `components/TodoList.tsx`

현재 그리드 `grid-cols-[20px_1fr_90px_70px_20px]`에 ✏ 버튼 열을 추가해 `grid-cols-[20px_1fr_90px_70px_24px_24px]`로 변경한다.

- [ ] **Step 1: `components/TodoItem.tsx` 수정**

`interface TodoItemProps`에 `onEdit` 추가:
```tsx
interface TodoItemProps {
  todo: Todo
  onToggle: (id: string, completed: boolean) => void
  onDelete: (id: string) => void
  onEdit: (todo: Todo) => void
}
```

함수 시그니처 변경:
```tsx
export function TodoItem({ todo, onToggle, onDelete, onEdit }: TodoItemProps) {
```

그리드 클래스를 다음으로 변경 (`grid-cols-[20px_1fr_90px_70px_20px]` → `grid-cols-[20px_1fr_90px_70px_24px_24px]`):
```tsx
<div
  className={`grid grid-cols-[20px_1fr_90px_70px_24px_24px] gap-3 px-3 py-2 rounded items-center group hover:bg-spotify-elevated transition-colors ${
    todo.completed ? 'opacity-[0.45]' : ''
  }`}
>
```

기존 삭제 버튼 바로 뒤에 ✏ 버튼 추가:
```tsx
      <button
        onClick={() => onDelete(todo.id)}
        aria-label="삭제"
        className="text-spotify-border hover:text-spotify-negative transition-colors text-xs opacity-0 group-hover:opacity-100"
      >
        ✕
      </button>

      <button
        onClick={() => onEdit(todo)}
        aria-label="수정"
        className="text-spotify-border hover:text-spotify-green transition-colors text-xs"
      >
        ✏
      </button>
```

- [ ] **Step 2: `components/TodoList.tsx` 수정**

`interface TodoListProps`에 `onEdit` 추가:
```tsx
interface TodoListProps {
  todos: Todo[]
  onToggle: (id: string, completed: boolean) => void
  onDelete: (id: string) => void
  onEdit: (todo: Todo) => void
}
```

함수 시그니처 변경:
```tsx
export function TodoList({ todos, onToggle, onDelete, onEdit }: TodoListProps) {
```

헤더 그리드 클래스 변경 (`grid-cols-[20px_1fr_90px_70px_20px]` → `grid-cols-[20px_1fr_90px_70px_24px_24px]`) 및 빈 열 추가:
```tsx
      <div className="grid grid-cols-[20px_1fr_90px_70px_24px_24px] gap-3 px-3 py-2 border-b border-spotify-card mb-1">
        <span className="text-sp-xs text-spotify-muted">#</span>
        <span className="text-sp-xs text-spotify-muted">제목</span>
        <span className="text-sp-xs text-spotify-muted text-center">마감일</span>
        <span className="text-sp-xs text-spotify-muted text-center">우선순위</span>
        <span />
        <span />
      </div>
```

TodoItem에 `onEdit` 전달:
```tsx
        <TodoItem key={todo.id} todo={todo} onToggle={onToggle} onDelete={onDelete} onEdit={onEdit} />
```

- [ ] **Step 3: TypeScript 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add components/TodoItem.tsx components/TodoList.tsx
git commit -m "feat: add edit button to TodoItem, wire onEdit prop through TodoList"
```

---

## Task 3: page.tsx에 수정 기능 연결

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: `app/page.tsx`에 import 추가**

파일 상단 import 목록에 추가:
```tsx
import { EditTodoModal } from '@/components/EditTodoModal'
```

- [ ] **Step 2: `editingTodo` 상태 추가**

`Home` 함수 내 기존 `useState` 선언들 아래에 추가:
```tsx
  const [editingTodo, setEditingTodo] = useState<Todo | null>(null)
```

- [ ] **Step 3: `handleEditSave` 함수 추가**

`handleDelete` 함수 바로 뒤에 추가:
```tsx
  const handleEditSave = async (
    id: string,
    data: { title: string; priority: Priority; dueDate: string | null; categoryId: string | null }
  ): Promise<boolean> => {
    const res = await fetch(`/api/todos/${id}`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    })
    if (!res.ok) { toast.error('수정에 실패했습니다'); return false }
    const updated: Todo = await res.json()
    setTodos((prev) => prev.map((t) => (t.id === id ? updated : t)))
    toast.success('할 일이 수정되었습니다')
    return true
  }
```

- [ ] **Step 4: TodoList에 `onEdit` 전달**

JSX에서 `<TodoList>` 컴포넌트를 다음으로 변경:
```tsx
              <TodoList
                todos={todos}
                onToggle={handleToggle}
                onDelete={handleDelete}
                onEdit={setEditingTodo}
              />
```

- [ ] **Step 5: `<EditTodoModal>` 렌더링 추가**

`{showForm && <AddTodoForm ... />}` 블록 바로 뒤에 추가:
```tsx
      {editingTodo && (
        <EditTodoModal
          todo={editingTodo}
          categories={categories}
          onSave={handleEditSave}
          onClose={() => setEditingTodo(null)}
        />
      )}
```

- [ ] **Step 6: TypeScript 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 7: 개발 서버에서 동작 확인**

```bash
npm run dev
```

브라우저에서 확인:
- Todo 행에 ✏ 버튼이 표시됨
- ✏ 클릭 시 모달이 열리고 기존 값이 pre-fill됨
- 저장 버튼 클릭 시 목록이 업데이트됨
- 취소 클릭 시 모달이 닫힘

- [ ] **Step 8: 커밋**

```bash
git add app/page.tsx
git commit -m "feat: wire edit modal in page.tsx with editingTodo state and handleEditSave"
```

---

## Task 4: E2E 테스트 추가

**Files:**
- Modify: `tests/e2e/todo.spec.ts`

- [ ] **Step 1: 수정 기능 E2E 테스트 추가**

`tests/e2e/todo.spec.ts` 파일 맨 끝에 다음 블록을 추가:

```typescript
test.describe('할 일 수정', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/')
  })

  test('수정 버튼 클릭 시 모달이 열린다', async ({ page }) => {
    let todoId: string | undefined
    try {
      todoId = await addTodoViaUI(page, '수정 모달 테스트')
      await page.locator('[aria-label="수정"]:visible').first().click()
      await expect(page.locator('form h2')).toHaveText('할 일 수정')
      await page.click('button:has-text("취소")')
    } finally {
      if (todoId) await page.request.delete(`/api/todos/${todoId}`)
    }
  })

  test('제목을 수정하고 저장하면 목록에 반영된다', async ({ page }) => {
    let todoId: string | undefined
    try {
      todoId = await addTodoViaUI(page, '수정 전 제목')
      await page.locator('[aria-label="수정"]:visible').first().click()
      await page.fill('input[placeholder="할 일을 입력하세요"]', '수정 후 제목')
      await page.getByRole('button', { name: '저장', exact: true }).click()
      await expect(page.locator('text=수정 후 제목').first()).toBeVisible()
    } finally {
      if (todoId) await page.request.delete(`/api/todos/${todoId}`)
    }
  })

  test('취소 버튼을 누르면 모달이 닫히고 내용이 변경되지 않는다', async ({ page }) => {
    let todoId: string | undefined
    try {
      todoId = await addTodoViaUI(page, '변경되지 않을 제목')
      await page.locator('[aria-label="수정"]:visible').first().click()
      await page.fill('input[placeholder="할 일을 입력하세요"]', '변경된 제목')
      await page.click('button:has-text("취소")')
      await expect(page.locator('form h2')).not.toBeVisible()
      await expect(page.locator('text=변경되지 않을 제목').first()).toBeVisible()
    } finally {
      if (todoId) await page.request.delete(`/api/todos/${todoId}`)
    }
  })
})
```

- [ ] **Step 2: 개발 서버가 실행 중인지 확인**

```bash
curl -s http://localhost:3000 > /dev/null && echo "서버 실행 중" || echo "서버를 먼저 실행하세요: npm run dev"
```

- [ ] **Step 3: E2E 테스트 실행**

```bash
npm run test:e2e
```

Expected: 전체 테스트 PASS (기존 테스트 포함)

- [ ] **Step 4: 커밋**

```bash
git add tests/e2e/todo.spec.ts
git commit -m "test: add E2E tests for todo edit feature"
```
